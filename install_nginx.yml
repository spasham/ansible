---
- name: install nginx server
  hosts: cloud-linux
  become: true
  tasks:
    - name: install nginx binaries
      dnf:
        name: nginx
        state: latest

    - name: start and enable nginx
      service:
        name: nginx
        state: started
        enabled: true

